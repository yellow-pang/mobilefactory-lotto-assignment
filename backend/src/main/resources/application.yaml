spring:
  application:
    name: lotto
  datasource:
    # url: ${DB_URL}
    # username: ${DB_USERNAME}
    # password: ${DB_PASSWORD}
    url: jdbc:mariadb://localhost:3306/lotto_event
    username: lotto_app
    password: passw0rd
    driver-class-name: org.mariadb.jdbc.Driver
  task:
    scheduling:
      pool:
        size: 2 # 스케줄 스레드 풀 크기
      thread-name-prefix: reminder- # 스레드 이름 프리픽스
      shutdown:
        await-termination: true # 종료 시 실행 중인 작업 완료 대기
        await-termination-period: 30s # 최대 대기 시간

# 테스트/개발 환경 설정
test:
  current-date: 2025-02-15 # 테스트용 고정 날짜 (이벤트 기간 내)

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.otr.lotto
  configuration:
    map-underscore-to-camel-case: true
